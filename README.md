# 作業本身審查（給外部專家）

線上位置：https://fefe9487.github.io/evluate_risk_assessment/

這站給外部專家標 **現用庫作業**（含十業新掛的三張卡）。每一筆可勾五分鐘核心、點句子標問題，也可另寫**綜合意見**（不限格式、不套標籤詞表）。引用背景來自正式 `industryMapping.json`。

只讀對照 **Fat 原版**：[fat.html](fat.html)。這頁不寫審查、不改 slim。六張後來才進 slim 的辦公／文書卡沒有 fat。

只讀對照 **專業版底稿**：[full.html](full.html)（Phase 1 第 1 階段 `writeback_full`，尚未做第 2 階段掃讀壓縮）。原批次 170 張；後來才進庫的三張（廳舍修繕、道路維護、高處繩索接近）已後補進去，頁面會標「後補」。

影視拍攝那張 2.0 庫混用「取代／消除／複合標」，不能當控制層級；Fat 頁改放 Gen4 完整 JSA，層級收到工程改善／行政管理／個人防護。

## 專家怎麼用

1. 用 Chrome / Edge 打開上面網址（不要下載 HTML 後雙擊，資料檔載不進來）。
2. 右上角填**審查人姓名**。
3. 每一筆可做三件事（可只做其中幾項）：
   - 勾五分鐘核心危害：**至少 2 類，沒有上限**（不要照抄「庫內核心」）。
   - 點有問題的危害情境、改善項目、核心理由或 **S 分數**，選標籤並用句子說明。沒問題的句子不必標。
   - 在各危害類型底下的 **不適當之處** 寫這組哪裡不適當（S、情境或對策皆可）；也可在右側寫**綜合意見**。
4. 定期按「備份進度」。全部或告一段落後按「匯出結果」，把 JSON 寄回。

瀏覽器會自動暫存。換電腦時請用「還原進度」上傳備份檔。

## 逐條標籤（封閉詞表，可多標）

| 標籤 | 意思 |
|------|------|
| 漏提 | 該有的情境或對策沒寫 |
| 空泛 | 寫了但現場做不到 |
| 不符行業 | 對不到本作業名稱／描述（工序錯置），不是「不該掛在某廠」 |
| 寫反 | 把誘發條件或禁令寫成對策，或對策與描述衝突 |
| 跨類複製 | 這句應屬別的危害組 |
| 過長 | 可刪可併的重複句 |
| 文風改良 | 寫得太機械、像套話，不像現場會講的話 |
| 不適當 | S 評分、情境或對策不適當 |

每組危害類型另有 **不適當之處** 欄位（`unsuitableNotes`），不必套標籤。綜合意見也不必用這些標籤。

危害情境左側顯示該條 **原始 S**（對不到 Map 原文時為 —，不發明分數）。組頭 **類型 S** 是該類型各情境原始 S 的最高值，不是加總。組內也會列出 Map 列序的原始 S。

## 匯出格式

審查結果為一個 JSON。`overallComment` 是綜合意見；`coreSet` 與 `itemNotes` 是勾選與逐條標記。

```json
{
  "schema": "ra-task-judge-phase1",
  "phase": 1,
  "annotator": "expert:王小明",
  "tasks": {
    "堆高機駕駛與搬運作業": {
      "taskId": "堆高機駕駛與搬運作業",
      "taskName": "堆高機駕駛與搬運作業",
      "coreSet": ["被撞", "物體倒塌、崩塌"],
      "overallComment": "整體可用。被夾不該佔核心。現場五分鐘應先看人車分道與堆高倒塌。",
      "unsuitableNotes": {
        "火災": "類型 S5 偏高，現場以電氣走火為主。"
      },
      "itemNotes": [
        {
          "groupName": "火災",
          "field": "AvoidItems",
          "index": 0,
          "quote": "原文整句",
          "tags": ["空泛", "不適當"],
          "problem": "用句子說明缺什麼、為什麼不好、應補什麼方向。"
        }
      ]
    }
  }
}
```

`index` 為 `null` 且 `quote` 為空字串，代表整組缺條、無法對到既有句子。未完成的作業仍會寫進檔案，並列在 `incomplete`。

## 維護者

- 作業正文：`data/library.json`（與 TOIRA 公開庫同步；`核心重點_existing` 是庫內旗，專家不要照抄）
- Fat 原版（只讀）：`data/library_fat.json` + `fat.html`
- 專業版底稿（只讀）：`data/library_full.json` + `full.html`（170 張 writeback_full + 後補三張）
- 行業掛載：`data/industryMapping.json`（與 TOIRA／給璞藝同一份）
- 比對腳本在主專案：`judge_draft/_compare_expert_phase1.py`
